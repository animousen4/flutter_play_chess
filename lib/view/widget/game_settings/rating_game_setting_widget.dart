import 'package:flutter/material.dart';

class RatingGameSettingWidget extends StatelessWidget {
  const RatingGameSettingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}